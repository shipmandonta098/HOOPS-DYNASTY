{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "player.schema.json",
  "title": "Player",
  "description": "A single basketball player or draft prospect. The same shape is used for rostered players, free agents, and undrafted prospects — the difference is just which fields are filled in (teamId null = free agent/prospect, contract null = unsigned).",
  "schemaVersion": 1,
  "type": "object",
  "required": [
    "id",
    "name",
    "position",
    "age",
    "attributes"
  ],
  "additionalProperties": true,
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1
    },
    "id": {
      "type": "string",
      "description": "Unique, stable player id (e.g. 'p_012' or 'prospect_2025_03'). Never reuse an id."
    },
    "name": {
      "type": "string"
    },
    "position": {
      "type": "string",
      "enum": [
        "PG",
        "SG",
        "SF",
        "PF",
        "C"
      ],
      "description": "Primary position. Drives how attributes are weighted into overall."
    },
    "age": {
      "type": "integer",
      "minimum": 15,
      "maximum": 50
    },
    "teamId": {
      "type": [
        "string",
        "null"
      ],
      "description": "Id of the team that rosters this player. null = free agent or undrafted prospect."
    },
    "college": {
      "type": "string",
      "description": "Fictional school, or 'Overseas Professional' / 'Developmental League'."
    },
    "heightIn": {
      "type": "integer",
      "minimum": 60,
      "maximum": 96,
      "description": "Height in inches."
    },
    "weightLb": {
      "type": "integer",
      "minimum": 120,
      "maximum": 400,
      "description": "Weight in pounds."
    },
    "birthDate": {
      "type": "object",
      "description": "Generated at league creation; year is derived from age and the starting season, so the two can never disagree.",
      "required": [
        "year",
        "month",
        "day"
      ],
      "additionalProperties": false,
      "properties": {
        "year": {
          "type": "integer"
        },
        "month": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12
        },
        "day": {
          "type": "integer",
          "minimum": 1,
          "maximum": 31
        }
      }
    },
    "birthplace": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "city": {
          "type": "string"
        },
        "region": {
          "type": [
            "string",
            "null"
          ],
          "description": "State/province, where one applies."
        },
        "country": {
          "type": "string"
        }
      }
    },
    "nationality": {
      "type": "string",
      "description": "Country the player represents. Comes from his birth country unless he was raised elsewhere."
    },
    "secondaryNationality": {
      "type": [
        "string",
        "null"
      ],
      "description": "Second country, for a player born in one and raised in another. Null for most players."
    },
    "namingOrigin": {
      "type": [
        "string",
        "null"
      ],
      "description": "Internal: the naming tradition his name was drawn from (a key in nameCultures.js). A naming tradition ONLY \u2014 not ancestry, not appearance, not ethnicity. Portrait or heritage systems must use their own separate field rather than reading this one. Null on saves made before naming cultures existed, where the country is not in the table."
    },
    "gender": {
      "type": "string",
      "description": "Internal: which given-name pool to draw from. 'male' throughout a men's league."
    },
    "experience": {
      "type": "integer",
      "minimum": 0,
      "description": "Completed seasons of service. Always age minus entry age, so it stays consistent with draft.year."
    },
    "draft": {
      "type": [
        "object",
        "null"
      ],
      "description": "Where the player was taken, or null if undrafted.",
      "required": [
        "year",
        "round",
        "pick"
      ],
      "additionalProperties": false,
      "properties": {
        "year": {
          "type": "integer"
        },
        "round": {
          "type": "integer",
          "minimum": 1
        },
        "pick": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "archetype": {
      "type": "string",
      "description": "Playing-style archetype id the ratings were shaped around (e.g. 'rim_protector'). See playerGen.js ARCHETYPES."
    },
    "archetypeLabel": {
      "type": "string",
      "description": "Display name for `archetype`, denormalised so screens need no lookup table."
    },
    "durability": {
      "type": "integer",
      "minimum": 0,
      "maximum": 99,
      "description": "Injury resistance, high is durable. Stored for the season simulator; no injury model consumes it yet, so it is shown as a trait rather than a risk percentage."
    },
    "morale": {
      "type": "integer",
      "minimum": 0,
      "maximum": 99,
      "description": "0..99. Set at generation; the season simulator may move it."
    },
    "fatigue": {
      "type": "integer",
      "minimum": 0,
      "maximum": 99,
      "description": "0..99, low is fresh. Set at generation; the simulator may move it."
    },
    "overall": {
      "type": "integer",
      "minimum": 0,
      "maximum": 99,
      "description": "DERIVED from attributes by engine/lib/ratings.js. Cached here for readability; treat attributes as the source of truth and recompute after edits."
    },
    "potential": {
      "type": "integer",
      "minimum": 0,
      "maximum": 99,
      "description": "Soft ceiling a young player can develop toward. For veterans this is roughly equal to overall."
    },
    "attributes": {
      "type": "object",
      "description": "0..99-scale skills, grouped into the five categories the game rates players on: Physical (strength, speed, vertical, agility, stamina, endurance), Shooting — the whole scoring game (layup, dunk, threePoint, midRange, freeThrow, postControl, shotIQ), Playmaking (passing, passingIQ, ballHandling), Defense (perimeterDefense, interiorDefense, steal, block, defensiveIQ) and Rebounding (offensiveRebound, defensiveRebound). This is the SOURCE OF TRUTH for ability — overall is computed from it.",
      "additionalProperties": false,
      "required": [
        "strength",
        "speed",
        "vertical",
        "agility",
        "stamina",
        "endurance",
        "layup",
        "dunk",
        "threePoint",
        "midRange",
        "freeThrow",
        "postControl",
        "shotIQ",
        "passing",
        "passingIQ",
        "ballHandling",
        "perimeterDefense",
        "interiorDefense",
        "steal",
        "block",
        "defensiveIQ",
        "offensiveRebound",
        "defensiveRebound"
      ],
      "properties": {
        "strength": {
          "$ref": "#/definitions/rating"
        },
        "speed": {
          "$ref": "#/definitions/rating"
        },
        "vertical": {
          "$ref": "#/definitions/rating"
        },
        "agility": {
          "$ref": "#/definitions/rating"
        },
        "stamina": {
          "$ref": "#/definitions/rating"
        },
        "endurance": {
          "$ref": "#/definitions/rating"
        },
        "layup": {
          "$ref": "#/definitions/rating"
        },
        "dunk": {
          "$ref": "#/definitions/rating"
        },
        "threePoint": {
          "$ref": "#/definitions/rating"
        },
        "midRange": {
          "$ref": "#/definitions/rating"
        },
        "freeThrow": {
          "$ref": "#/definitions/rating"
        },
        "postControl": {
          "$ref": "#/definitions/rating"
        },
        "shotIQ": {
          "$ref": "#/definitions/rating"
        },
        "passing": {
          "$ref": "#/definitions/rating"
        },
        "passingIQ": {
          "$ref": "#/definitions/rating"
        },
        "ballHandling": {
          "$ref": "#/definitions/rating"
        },
        "perimeterDefense": {
          "$ref": "#/definitions/rating"
        },
        "interiorDefense": {
          "$ref": "#/definitions/rating"
        },
        "steal": {
          "$ref": "#/definitions/rating"
        },
        "block": {
          "$ref": "#/definitions/rating"
        },
        "defensiveIQ": {
          "$ref": "#/definitions/rating"
        },
        "offensiveRebound": {
          "$ref": "#/definitions/rating"
        },
        "defensiveRebound": {
          "$ref": "#/definitions/rating"
        }
      }
    },
    "mental": {
      "type": "object",
      "description": "Mental attributes on the same 1-99 scale as ability ratings, but DELIBERATELY OUTSIDE `attributes` so they can never feed `overall` — overall is computed by iterating the ability categories over `attributes` alone. These describe how a player responds to pressure, mistakes, opponents and coaching. They are tendencies that shift probabilities, never guarantees.",
      "required": [
        "resilience",
        "concentration",
        "confidence",
        "composure",
        "coachability"
      ],
      "additionalProperties": false,
      "properties": {
        "resilience": {
          "$ref": "#/definitions/mentalRating",
          "description": "Recovery from mistakes, missed shots and bad stretches."
        },
        "concentration": {
          "$ref": "#/definitions/mentalRating",
          "description": "Sustained engagement; fewer lapses and careless errors."
        },
        "confidence": {
          "$ref": "#/definitions/mentalRating",
          "description": "Willingness to take responsibility under pressure. Does NOT improve shooting ability."
        },
        "composure": {
          "$ref": "#/definitions/mentalRating",
          "description": "Emotional control against crowds, trash talk, physicality and officials."
        },
        "coachability": {
          "$ref": "#/definitions/mentalRating",
          "description": "Receptiveness to feedback, systems and role changes."
        }
      }
    },
    "personality": {
      "type": "object",
      "description": "Layer three: what kind of person the player is to manage. Like `mental`, this sits OUTSIDE `attributes` so it can never feed `overall` — a difficult 91-overall star is still a 91. Traits describe who he is and barely change; priorities describe what currently matters and are derived from his traits, age and standing, so they evolve as his career does.",
      "required": [
        "traits",
        "priorities"
      ],
      "additionalProperties": false,
      "properties": {
        "traits": {
          "type": "array",
          "description": "Usually 2-5 trait ids from playerPersonality.js TRAITS. Mutually exclusive axes (stability, ego, tolerance, expression, leadership) never both appear.",
          "items": {
            "type": "string"
          }
        },
        "priorities": {
          "type": "object",
          "description": "0-100 weights per career priority. Inputs to decisions, never the decision itself; the five display levels are derived from the number.",
          "additionalProperties": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          }
        }
      }
    },
    "relationships": {
      "type": "array",
      "description": "Relationships with teammates, coaches, GMs and organisations. Empty until events exist to build them — relationships are meant to grow through what actually happens, not to be assigned at generation from personality.",
      "items": {
        "type": "object",
        "required": [
          "withId",
          "kind",
          "standing"
        ],
        "additionalProperties": true,
        "properties": {
          "withId": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "teammate",
              "coach",
              "gm",
              "organization"
            ]
          },
          "standing": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          }
        }
      }
    },
    "contract": {
      "type": [
        "object",
        "null"
      ],
      "description": "Current contract, or null if unsigned/free agent.",
      "required": [
        "salary",
        "yearsRemaining"
      ],
      "additionalProperties": true,
      "properties": {
        "salary": {
          "type": "number",
          "minimum": 0,
          "description": "Annual salary in $M."
        },
        "yearsRemaining": {
          "type": "integer",
          "minimum": 0
        },
        "type": {
          "type": "string",
          "enum": [
            "rookie",
            "standard",
            "max",
            "vet_min"
          ],
          "description": "Contract archetype. Advisory — expand as you add cap rules."
        },
        "playerOption": {
          "type": "boolean"
        },
        "teamOption": {
          "type": "boolean"
        }
      }
    },
    "statsHistory": {
      "type": "array",
      "description": "Per-season box-score lines, appended by simulateSeason.js.",
      "items": {
        "type": "object",
        "required": [
          "season"
        ],
        "additionalProperties": true,
        "properties": {
          "season": {
            "type": "integer"
          },
          "ppg": {
            "type": "number"
          },
          "rpg": {
            "type": "number"
          },
          "apg": {
            "type": "number"
          }
        }
      }
    },
    "draftClass": {
      "type": "integer",
      "description": "Prospects only: the year of the draft they belong to."
    },
    "tier": {
      "type": "string",
      "description": "Prospects only: talent tier label (franchise/star/starter/rotation/fringe)."
    },
    "projectedPick": {
      "type": "integer",
      "description": "Prospects only: scouting board position."
    }
  },
  "definitions": {
    "rating": {
      "type": "integer",
      "minimum": 0,
      "maximum": 99,
      "description": "A single 0..99 skill rating."
    },
    "mentalRating": {
      "type": "integer",
      "minimum": 1,
      "maximum": 99,
      "description": "A single 1..99 mental rating."
    }
  }
}
